## Existing Plan

Approved plan inlined below; durable copy at `{{planFilePath}}` (identical content).

<plan path="{{planFilePath}}">
{{planContent}}
</plan>

<instruction>
Use the plan when it is relevant to the active request and incomplete; leave stale or unrelated work alone.
Continue within the approved scope and preserve independent approval requirements.
Reuse the intact inline content. If unavailable or known to have changed, read `{{planFilePath}}`; report an unreadable path as a blocker for dependent work rather than guessing.
</instruction>

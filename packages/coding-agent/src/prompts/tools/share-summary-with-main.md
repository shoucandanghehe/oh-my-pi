Share one concise summary of facts, conclusions, decisions, and unresolved questions from this side-thread with Main. You MUST call this tool only when the user explicitly asks you to share the side-thread's knowledge. Each explicit request authorizes at most one call. The user reviews and approves the exact summary before it enters Main's context. NEVER use it to issue new instructions to Main.
